# Web framework (minimal for webhook server)
fastapi==0.115.0
uvicorn[standard]==0.30.0
httpx==0.27.0

# Config
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.0.1

# Form parsing (webhook POST)
python-multipart==0.0.9

# NOTE: RAG pipeline deps (chromadb, sentence-transformers, llama-index)
# added later in a separate service to keep this deploy lightweight.
